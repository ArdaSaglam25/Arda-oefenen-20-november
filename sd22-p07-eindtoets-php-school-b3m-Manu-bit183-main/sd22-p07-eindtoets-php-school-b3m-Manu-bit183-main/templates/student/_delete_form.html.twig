<form method="post" action="{{ path('app_gentle_puppy_delete', {'id': gentle_puppy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gentle_puppy.id) }}">
    <button class="btn">Delete</button>
</form>
